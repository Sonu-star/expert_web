import 'package:expertgraphweb/global_export.dart';

class Div extends StatelessWidget {
  const Div({super.key});

  @override
  Widget build(BuildContext context) =>
      Container(width: 1, height: Get.height, color: grayLightest);
}
